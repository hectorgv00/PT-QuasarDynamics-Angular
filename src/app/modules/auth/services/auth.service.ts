import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class AuthService {
  constructor() {}

  public sendCredentials(body: any): void {
    try {
      
    } catch (error) {}
  }
}
